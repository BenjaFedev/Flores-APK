name: Compilar APK
on: [push, workflow_dispatch]

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Compilar
        run: |
          unzip JardinAmarillo.zip
          cd JardinAmarillo
          gradle :app:assembleDebug --no-daemon
      - uses: actions/upload-artifact@v4
        with:
          name: Flores-para-mi-nina-APK
          path: JardinAmarillo/app/build/outputs/apk/debug/app-debug.apk
